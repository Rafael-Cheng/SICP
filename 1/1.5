if it uses normal-order, (p) will be valued forever, therefore it will never stop.
if it uses applicative-order, (p) won't be valued until it has to. As we can see x equals to 0, for test procedure, it returns when x equals 0, accordingly, the output is 0.
